# Reporte de sesión — 25 de septiembre de 2026

Continuación de la sesión del 24-sep (`2026-09-24.md`). Commits del día, en orden: `d919539` (cierre del 24, recarga del núcleo en Cloud), `12c7a76`, `61ec35d`, `faaab61`, `188815c`, `4cac39b`, `93ed014`, `3c4cdd1`, `0639002`, `4cbbb2b`, `8bc438d`. Todo está en `origin/master`.

El día tuvo dos mitades: primero se cerró el último pendiente técnico del modelo (#7, la pasada 3). Después, con la app dada por terminada por el profesor, el trabajo pasó a **preparar el material para redactar los informes** con otro chat de Claude.

---

## 1. Validación en Cloud del arreglo de recarga

El usuario confirmó que, tras el push de `d919539`, la app en Cloud volvió a funcionar sin reiniciarla: el núcleo (`src/`) se recarga solo al detectar cambios.

## 2. Pendiente #7 investigado y resuelto: la pasada 3 no optimizaba nada (`12c7a76`, `61ec35d`, `faaab61`, `188815c`)

**Investigación.** El gap de ~64% de la pasada 3 no venía de una relajación débil, como se creía. HiGHS devolvía **sin cambios** su punto de partida (la solución de la pasada 2), que además traía `peso_min = 0`: el "desbalance" informado (9.068 t) era el peso de la bodega más cargada, y el real era 6.294 t. Con el punto de partida corregido, e incluso aflojando restricciones, HiGHS tampoco mejoraba. Una búsqueda por vecindarios (liberar dos bodegas y fijar las otras seis) llegó a 3.604 t en ~30 s.

**Implementación** (pedida por el usuario tras ver la investigación): `resolver_pasada3_por_vecindarios` en `modelo_prestow.py`, que usan la web y el CLI. Ajusta el punto de partida, corre 10 s de HiGHS solo para obtener la cota dual (y así informar un gap honesto) y recorre los pares de bodegas hasta que ninguno mejore, verificando cada solución contra todas las restricciones. En la primera versión, a 30 s, la pasada 3 subía el makespan de 59,97 a 60,21 h al aprovechar el margen de la pasada 2. Se corrigió acotando `T_max` al valor de la pasada 2.

**Resultado del caso base (180 s, dos corridas idénticas):** makespan 59,67 h sin cambio; desbalance de peso 6.294 → 3.604 t; gap de la pasada 3 63,91% → 9,19%; desbalance entre cuadrillas 9,4% → **5,8%** (mejor que el 6,9% del plan manual); fragmentación 11; 1.836 izadas. Se actualizó la tabla de CLAUDE.md sección 3.

**Prueba en Cloud:** el usuario corrió la app y obtuvo 59 h 58 min, 1,4% de desbalance y fragmentación 31. Coincide con una corrida a **30 s** por pasada, no a 180 s: es válida, pero con poco tiempo la pasada 2 no alcanza a agrupar la carga. El usuario decidió no mejorar más la app.

## 3. Documento base para los informes (`4cac39b`, `0639002`, `4cbbb2b`, `8bc438d`)

`docs/reportes_sesion/2026-09-25_explicacion_completa_para_informes.md`: explicación completa del sistema, pensada para que la lea **otro chat de Claude** que redacte los informes Técnico y Académico.

- **Sección 0:** instrucciones para ese chat: archivos a adjuntar, reglas de cifras, no decir "óptimo", no inventar datos ni citas, extensión por sección y marcador `[PENDIENTE EQUIPO: T#]`.
- **Secciones 1-12:** contenido común (sistema, problema, datos, supuestos, modelo, método, arquitectura, web con la tabla de campos, validación, resultados, limitaciones, cronología).
- **Secciones 13-14:** guía subtarea por subtarea de cada informe, con tablas de decisiones y de errores y sus aprendizajes para el Académico.
- **Sección 15:** registro de cifras obsoletas de los documentos anteriores (57,59 h, 58,19 h, desbalance 0,1%, "275 sin reconciliar", etc.).
- **Sección 16:** tareas que solo el equipo puede hacer (T1-T5, ver abajo).
- **Sección 18:** análisis hechos para los informes.

Al revisar las guías de los informes se detectó que ambos exigen **instancias de prueba**, **sensibilidad a los rendimientos t/h** (la guía la llama "el análisis obligatorio") y **sensibilidad al tamaño**, y que ninguno existía. No son trabajo de la app sino material de los informes.

## 4. Análisis para los informes (`93ed014`, `3c4cdd1`)

- `src/generador_instancias.py`: genera variantes del caso base por perturbación (cada instancia cambia una cosa y responde una pregunta).
- `src/analisis_informes.py`: las corre con la misma función que usa la web.
- Datos en `data/instancias/` (17 instancias) y resultados en `data/resultados_analisis/resultados.csv` (21 corridas).

Hallazgos:

1. **La regla de 180 s es frágil.** Desde 180 s la pasada 1 no usa punto de partida, y 3 de 8 instancias (P3, P6, R_b1_180) terminaron **sin solución**. A 170 s resolvieron las 12, todas con las 4 verificaciones OK. Quedó documentado como limitación, con la recomendación de 180 s para el caso demo y 170 s para otros casos. No se corrigió en la app, por decisión del usuario.
2. **Rendimientos t/h:** ±10% de rendimiento general mueve el makespan ∓10%. Con la bodega 1 a 270 t/h, el makespan baja 4,19 h (−7%).
3. **Tamaño:** hasta 10 bodegas y 5 destinos, gap de makespan ≤ 4,4%. Con 12 bodegas y 6 destinos, el makespan sigue bien (4,3%), pero la pasada 2 no converge y la fragmentación queda en 70.
4. **El óptimo del caso base está entre 57,49 y 58,24 h**, acotado con P1 (un solo destino, una versión con menos restricciones) y P6 (seis destinos, cuyo plan también es válido para el caso base).
5. P7 (carga al 130%) se rechaza al instante con un mensaje claro.

Tests: 52/52 (se sumaron los del generador).

## 5. Tareas del equipo (sección 16 del documento base)

| # | Tarea |
|---|---|
| T1 | Verificar las referencias APA 7 en la fuente original (y documentar la búsqueda de literatura) |
| T2 | Tomar las capturas de la app desplegada con el caso demo a 180 s (59 h 40 min) |
| T3 | Llenar o reconstruir la bitácora semanal |
| T4 | Escribir las autoevaluaciones individuales |
| T5 | Conseguir el enunciado oficial del curso y adjuntarlo al chat que redacte |

## Qué queda

- Las tareas T1-T5, del equipo.
- Redactar los informes con otro chat. Hay que adjuntarle:
  - el documento base;
  - `docs/Guia_Informe_Tecnico_Prestow_Lirquen.pdf` y `docs/Guia_de_tareas_Prestow_Lirquen.pdf`;
  - `docs/Restricciones_Modelo_Prestow_Lirquen.pdf`;
  - el enunciado del curso;
  - la bitácora.
- Supuestos 1-3 (tonelaje por bodega, rendimientos, pares de cuadrillas) siguen sin confirmar con el puerto.
- Solo si se pide, a nivel técnico: el reintento con punto de partida cuando la pasada 1 no encuentra solución a 180 s, y el balance de peso puerto a puerto con vecindarios.
