# Dependencias de ejecución (lo que instala Streamlit Community Cloud).
# Las de desarrollo (tests, lint) están en requirements-dev.txt.

# Núcleo de optimización
# pulp < 4: el código usa prob.constraints como dict (nombre -> restricción),
# que PuLP 4.0 elimina (ver DeprecationWarning de PuLP 3.x).
pulp>=2.8.0,<4
highspy>=1.7.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Manipulación de datos
pandas>=2.2.0
numpy>=1.26.0

# Interfaz web
streamlit>=1.32.0

# Visualización de planimetría
matplotlib>=3.8.0
plotly>=5.20.0
